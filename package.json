{
  "name": "ejecafetero",
  "version": "0.1.0",
  "private": true,
  "description": "Tour virtual inmersivo del Valle del Cócora / Eje Cafetero — WebXR + desktop",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke-tour.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "playwright": "^1.63.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
